{
    "name": "bbgpetpro-root",
    "private": true,
    "scripts": {
        "build": "npm run build:landing && npm run build:crm && npm run build:tutor && npm run build:admin",
        "build:landing": "cd bbgpet-landing && npm install && npx vite build --outDir ../dist --base / --emptyOutDir",
        "build:crm": "cd crm && npm install && npx vite build --outDir ../dist/flowpetpro --base /flowpetpro/ --emptyOutDir false",
        "build:tutor": "cd flowpetAPPdoCliente && npm install && npx vite build --outDir ../dist/tutor --base /tutor/ --emptyOutDir false",
        "build:admin": "cd flow-pet-pro-Super-Admin && npm install && npx vite build --outDir ../dist/admin --base /admin/ --emptyOutDir false"
    }
}